okay.. this is gonna be daunting

so far it's looking like my best path forward is Alpine.js. it's a nice lightweight javascript framework that handles updating the DOM much better than raw javascript, in a much cleaner way. and there's no React bloat

for server-side stuff i could use either Alpine or perhaps HTMX. we'll see

i should also clean up the backend webui.py while i'm at it, since that too is largely AI generated. less of a pain to maintain, but still, could use a good cleanup

## TODO
- recreate the settings panel in alpine.js
- re-add theme support
- add in-chat search functionality back
- move chat exporting to core, make it work with the webui's modal
- recreate the storage editor
- recreate global search modal
- add tag filter and global search back to chat list
